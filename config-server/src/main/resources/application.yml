server:
  port: 9900

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/uriseozz/config-setting.git # git repo 주소
          default-label: main
          search-paths: eureka-server # 마이크로서비스 이름
