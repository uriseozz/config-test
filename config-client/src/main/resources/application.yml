server:
  port: 8800

spring:
  application:
    name: config-client
  profiles:
    active: dev
  config:
    import: optional:configserver:http://localhost:9900 # config server

management: #actuator
  endpoints:
    web:
      exposure:
        include: refresh #RefreshScope 설정 사용